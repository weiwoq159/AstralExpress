import { Typography } from "antd";

const { Text } = Typography;

export const PortalFooter = () => {
  return <Text style={{ fontSize: 14, color: "#526b82", lineHeight: 1, margin: 0 }}>愿此行，终抵群星</Text>;
};
